import getStyles from "../../Style";

interface Props {
  firstPage: boolean;
}

function ContactInfo({ firstPage }: Props) {
  return (
    <div>
      <div></div>
    </div>
  );
}

const styles = {};

export default ContactInfo;
