import getStyles from "../../Style";

interface Props {
  firstPage: boolean;
}

function GeneralInfo({ firstPage }: Props) {
  return (
    <div>
      <div></div>
    </div>
  );
}

const styles = {};

export default GeneralInfo;
